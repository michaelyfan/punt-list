# punt_list

Punt List is a list app which lets you move items between lists with one tap.

## Running

```bash
flutter run -d chrome
```

To pre-seed the app with test data, set `seedTestData = true` in `lib/config.dart` before running.
